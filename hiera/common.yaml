---
classes:
    - apt
    - ssh
    - sudoers
    - zsh

user:
    iszak:
        ensure: present
        require: Class[zsh]
        managehome: true
        shell: /bin/zsh

zsh::config:
    iszak:
        owner: iszak
        group: iszak

sudoers::config:
    iszak:
        user: iszak